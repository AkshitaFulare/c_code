#include<stdio.h>
int main( )
{
  int side,peri,area;
  side=6;
  peri=2*side;
  printf("\n perimeter of square with side %d is= %d ",side,peri);
  area=side*side;
  printf("\n Area of square with side %d is= %d ",side,area);
return 0;
}

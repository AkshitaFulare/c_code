#include <iostream>
#include <conio.h>
#include <map>
#include <iterator>
using namespace std;

sort(array.begin(),array.end());
        for(int i=0;i<n;i++){
            if(array[i]!=i+1){
                return i+1;
            }
        }
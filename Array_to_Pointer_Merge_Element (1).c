#include<stdio.h>
int main()
{
    int arr[100];
    int size1,size2,intNum,arr1,arr2;
    printf("this is program to merge two sorted array ");
    printf("Enter the size of array1: \n");
    scanf("%d",&size1);
    printf("Enter the number of element :  ");
    for(index=0;index<size1;index++)
    {
        printf("Enter the element in array1",index+1);
        scanf("%d",&arr1[index]);
    }
    printf("Enter the size of array2: \n");
    scanf("%d",&size2);
    for(int index2=0;index2<size;)
    {
        for(int index3=0;index3<size)
        {
            if(arr1[index2]<arr2[index2])
        }
    }

}
